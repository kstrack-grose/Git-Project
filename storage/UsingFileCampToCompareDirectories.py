#!/usr/bin/python
# CHERNOH

import filecmp, dircmp #for comparting both files and directorys
filecmp.cmp()
filecmp.dircmp('latestDirectory','newDirectory'[ignore[,hide]]) #now we want to compare these two directories' contents

# filters and ignores if no change/difference


